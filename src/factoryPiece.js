function factoryPiece(player, colour, type, currentPos) {
    return {
        player,
        colour,
        type,
        currentPos,
        possibleMoves: [],
    }
}

// export default factoryPiece;
